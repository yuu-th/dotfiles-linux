# 日本語入力（fcitx5）— SETUP.md Phase 4-1 / 4-4
#
# GTK_IM_MODULE は意図的に設定しない。
#   未設定だと GTK3/GTK4 は内蔵の Wayland IM モジュール（text-input-v3）を使う。
#   fcitx を設定すると niri #454 で一部アプリの候補ポップアップが出なくなる。
#   ちらつきが我慢できない場合のみ GTK_IM_MODULE=fcitx を検討する。

XMODIFIERS=@im=fcitx
QT_IM_MODULES=wayland;fcitx
